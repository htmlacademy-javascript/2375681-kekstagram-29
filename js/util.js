const getRandomInteger = (min, max) => Math.floor(Math.random() * (max - min + 1)) + min;
// const newComment = comment.cloneNode(true);
    // newComment.querySelector('.sotial__picture').src = comment.avatar;
    // newComment.querySelector('.sotial__picture').alt = comment.name;
    // newComment.querySelector('.sotial-text').textContent = comment.message;

    // commentsFragment.appendChild(newComment);
export {getRandomInteger};
